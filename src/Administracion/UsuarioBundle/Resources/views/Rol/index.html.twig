{% extends '::administracion.html.twig' %}

{% block title %}Inicio - Telesur{% endblock %}

{% block titulobanner %}
    ADMINISTRACIÓN DE ROLES
{% endblock %}

{% block body %}
    {{ parent() }}

    <div class="titulo">LISTA DE ROLES</div>

    <table cellpadding="0" cellspacing="0" border="0" class="display" id="tablalista">
        <thead>
            <tr>
                <th>Id</th>
                <th>Rol</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('rol_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.rol }}</td>
                <td>

                    <a href="{{ path('rol_show', { 'id': entity.id }) }}"><span class="icon-search"></a>
                    <a href="{{ path('rol_edit', { 'id': entity.id }) }}"><span class="icon-edit"></a>
            
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <br><br>

    <a class="btn btn-primary" href="{{ path('rol_new') }}">Nuevo</a>

    <br><br>

{% endblock %}