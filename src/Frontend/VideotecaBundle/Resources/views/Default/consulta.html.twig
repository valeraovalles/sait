{% extends '::videoteca.html.twig' %}

{% block title %}Inicio - Telesur{% endblock %}

{% block titulobanner %}
    PAGINA PRINCIPAL VIDEOTECA
{% endblock %}

{% block body %}

    {{ parent() }}

        <div class="titulo">HISTÓRICO SISTEMA AVILA</div>

        <table class="table table-striped" style="font-size:12px">
        	<thead>
	        	<tr>
                    <th>Id segmento</th>
	        		<th>Serial</th>
	        		<th>Titulo</th>
                    <th>Contenido Segmento</th>
                    <th>Capitulo</th>
	        	</tr>
        	</thead>

        	<tbody>
        		{% for entity in cintas %}
        		<tr>
                    <td>{{ entity['id_segmento'] }}</td>
        			<td>{{ entity['serial_cinta'] }}</td>
        			<td>{{ entity['titulo'] }}</td>
                    <td>{{ entity['contenido_segmento'] }}</td>

                    <td>{{ entity['capitulo'] }}</td>
        		</tr>
        		{% endfor %}	
        	</tbody>
        </table>

        <a class="btn" href="{{ path('videoteca_homepage') }}">Ir a la lista</a>
        <br>
{% endblock %}

         