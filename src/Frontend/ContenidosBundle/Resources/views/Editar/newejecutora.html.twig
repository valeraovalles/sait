{% extends '::contenidos.html.twig' %}

{% block titulobanner %}
    UNIDAD EJECUTORA
{% endblock %}

{% block body -%}
    {{ parent() }}
    <div class="titulo">Unidad Ejecutora</div>

    <form action="{{ path('createejecutora') }}" method="post" {{ form_enctype(form) }}>
       
        <div id="descripcion" class="form-show_edit">
            <div class="contenedor">
                <br>
                {{ form_errors(form.nombre) }}
                <div class="labels">Nombre de Unidad Ejecutora:</div>
                <div class="widgets">{{ form_widget(form.nombre) }}</div>
            </div>  
            <br>     
            <button class="btn" type="submit">Crear</button>
            <br><br>
        </div>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('indexejecutora') }}">
            <img width="85px" src="{{ asset('images/listado.jpg')}}">
        </a>
    </li>
</ul>
{% endblock %}
