{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Controlpagounidad edit</h1>

    <form novalidate="novalidate" action="{{ path('controlpagounidad_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>{{ form_widget(edit_form._token) }}
        <input type="hidden" name="_method" value="PUT" />
        {{ form_widget(edit_form) }}
        <p>
            <button type="submit">Edit</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('controlpagounidad') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('controlpagounidad_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
