<div class="menubootstrap">
  <div align="left">
    <nav class="navbar navbar-inverse" role="navigation">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
          <a class="navbar-brand" href="{{ path('usuario_homepage') }}"><span style="color:white;" class="glyphicon glyphicon-home"></span></a>
      </div>
      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
            <li><a href="{{ path('transporte') }}"><span>INICIO</span></a></li>
            {% if is_granted('ROLE_TRANSPORTE_ADMINISTRADOR') %}
              <li class="dropdown">
                 <a href="#" class="dropdown-toggle" data-toggle="dropdown">ADMINISTRACI&Oacute;N <b class="caret"></b></a>
                 <ul class="dropdown-menu">
                   <li><a href="{{ path('vehiculos_list') }}">VEHICULOS</a></li>
                   <li><a href="{{ path('personalexterno_list') }}">PERSONAL EXTERNO</a></li>
                 </ul>
              </li>
            {% endif %}
            <li class="dropdown">
               <a href="#" class="dropdown-toggle" data-toggle="dropdown">SOLICITUDES<b class="caret"></b></a>
               <ul class="dropdown-menu">
                  {% if is_granted('ROLE_TRANSPORTE') %} 
                    <li><a href="{{ path('solicitudes_new') }}">CREAR</a></li>
                    <li><a href="{{ path('missolicitudes') }}"><span>MIS SOLICITUDES</span></a></li>
                  {% endif %}
                  {% if is_granted('ROLE_TRANSPORTE_ADMINISTRADOR') %} 
                    <li><a href="{{ path('solicitudes') }}">ADMINISTRAR</a></li>
                  {% endif %}                  
               </ul>
            </li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <li><a href="{{ path('contacto') }}">CONTACTO</a></li>
          <li><a href="{{ path('usuario_logout') }}">SALIR</a></li>
        </ul>
      </div><!-- /.navbar-collapse -->
    </nav>
  </div>
</div>