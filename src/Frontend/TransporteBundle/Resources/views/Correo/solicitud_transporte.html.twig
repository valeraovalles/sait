<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
        <style type="text/css">
        	th{
        		text-align: right;
        		margin-right: 20px;
        	}

        	.solicitud{
        		text-align: justify;
        	}

        	table{
        		width: 600px;

        	}

        	table td{
        		padding: 10px;
        		border:1px solid gray;
        	}

        	table th{
        		border:1px solid gray;
        		background-color: #d9edf7;
        		padding-right: 10px;
        		width: 25%;
        	}
        </style>
    </head>

    <body>
    	<div align="center">
            {% if status=="N" %}
                <div><h3>NUEVA SOLICITUD DE TRANSPORTE</h3></div>            
                <h3 align="center">Se ha generado una nueva solicitud de Transporte, bajo los siguientes datos:</h3>            
            {% endif %}
            {% if status=="AP" %}
                <div><h3>APROBACION DE SOLICITUD DE TRANSPORTE</h3></div>            
                <h3 align="center">Se ha aprobado la siguiente Solicitud de Transporte:</h3>            
            {% endif %}
            {% if status=="C" %}
                <div><h3>CULMINACION DE SOLICITUD DE TRANSPORTE</h3></div>            
                <h3 align="center">Se ha finalizado/culminado la siguiente Solicitud de Transporte:</h3>            
            {% endif %}
            {% if status=="R" %}
                <div><h3>RECHAZO DE SOLICITUD DE TRANSPORTE</h3></div>            
                <h3 align="center">Se ha rechazado la siguiente Solicitud de Transporte:</h3>            
            {% endif %}

			<table cellspacing=0>
				<tr>
					<th>Solicitante:</th>
					<td>{{ perfil.primerNombre|title }} {{ perfil.primerApellido|title }}</td>
				</tr>
           	<tr>
					<th>Motivo de Solicitud:</th>
					<td>{{ solicitud.datosInteresRazon }}</td>
				</tr>
				<tr>
					<th>Fecha de Salida:</th>
					<td>{{ solicitud.fechaSalida|date('d-m-Y') }}</td>
				</tr>
                <tr>
                    <th>Hora de Salida:</th>
                    <td>{{ solicitud.horaSalida|date('G:i:s') }}</td>
                </tr>
                <tr>
                    <th>Desde:</th>
                    <td>{{ solicitud.direccionDesde }}</td>
                </tr>
                <tr>
                    <th>Hasta:</th>
                    <td>{{ solicitud.direccionHasta }}</td>
                </tr>
                
                <tr>
                    <th>Estatus:</th>
                    {% if status=="N" %}
                        <td>Nueva</td>
                    {% endif %}
                    {% if status=="AP" %}
                        <td>Aprobada</td>
                    {% endif %}
                    {% if status=="C" %}
                        <td>Culminada</td>
                    {% endif %}
                    {% if status=="R" %}
                        <td>Rechazada</td>
                    {% endif %}
                </tr>

			</table>
		</div>
        <p align="center">
            <i><b>NOTA:</b> Para una mayor información consulte el módulo de Transporte del Sistema de Aplicaciones Internas.</i>
        </p>


	</body>
</html>
