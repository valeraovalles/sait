{% extends '::mercal.html.twig' %}
 
{% block title %}Numeraci&oacute;n{% endblock %}

{% block titulo %}
    NUMERACI&Oacute;N
{% endblock %}

{% block ubicacion %}
<ol class="breadcrumb">
  <li><a href="{{ path("usuario_homepage") }}">INICIO</a></li>
  <li><a href="{{ path("mercal_seleccionajornada") }}">SELECCIONAR JORNADA</a></li>
  <li class="active">NUMERACI&Oacute;N</li>
</ol>
{% endblock %}

{% block body -%}
    {{ parent() }}
    
    {#
    <div class="titulojornada">JORNADA {{ jornada.nombrejornada|upper }}</div>

    <div id="numeracion"> 
        <div class="numero">{{ datos.numero }}</div>
        <div class="nombre">SUSPENDIDA</div>
        <div class="cedula">HASTA LA SEMANA DEL 05/05/2014</div>
    </div>
    #}
    
    <div id="numeracion"> 
        <div class="numero">{{ datos.numero }}</div>
        <div class="nombre">{{ datos.nombre }}</div>
        <div class="cedula">{{ datos.cedula }}</div>
    </div>

    <input type="hidden" id="idjornada" value="{{ jornada.id }}">


    
    <script type="text/javascript">
            function consultaAjax(){
                var idjornada = $("#idjornada").val();
                var ruta = "{{ path('ajaxmercal_siguienteautomatico', {'idjornada':'valoridjornada'}) }}";
                ruta = ruta.replace("valoridjornada", idjornada);
                $('#numeracion').load(ruta);
            };
            setInterval(consultaAjax, 3000 );
    </script>

{% endblock %}